const Tag=({tags})=>{
    return(
        <div>tags.map((tag)=>{
            <spen key (key=key={tag}>{'#' + tag}</span>   // '#'(샵)과 tag 값을 붙여 표시 → #react
      ))}
    </div>
  )
}
export default Tag
    )
}