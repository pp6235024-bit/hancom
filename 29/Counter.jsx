import{useState}from"react";
const Counter= )=>{
    const[count, setCount]=use StaticRange(0)
    return(
        <vutton onClick={()=>setCount(c=>c+1)}>
        {count}번눌럿어요
        </button>
    )
}
export default Counter}